// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.pexels.com', 'randomuser.me'], // Update to include the specific image domain
    },
  };
  
  export default nextConfig;
  